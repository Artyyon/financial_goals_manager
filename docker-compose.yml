services:
  atlas-life:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./db/Production:/app/db/Production
      - ./key:/app/key:ro
    env_file:
      - .env
    restart: unless-stopped
